# @deprecated 2026-09-05：此 .env 已棄用，請遷至 phytotrack.toml（見 backend/phytotrack.toml.example）
# 僅 AI_API_KEY 仍支援以 env AI_API_KEY 覆蓋 TOML，其餘請改 phytotrack.toml
# 為相容舊部署，若 backend/.env 仍存在將印 WARN 並忽略（除 AI_API_KEY 外）
#
# 機台特定 AI 設定範本（舊）：複製為 backend/.env 後依本機環境修改。
# .env 已被 .gitignore 排除，勿提交實際內容。
#
# AI_BASE_URL：llama-server / LlamaStash proxy 的位址（不含 /v1）。
#             同時驅動 spring.ai.openai.base-url 與 app.ai.health-url。
AI_BASE_URL=http://localhost:11435
# AI_MODEL：需對應 proxy /v1/models 回傳的模型 id（llama-server 用載入檔名）。
AI_MODEL=qwen_qwen2.5-coder-3b-instruct-q8_0
# AI_API_KEY：llama.cpp 不驗證 key，僅滿足 Spring AI 必填欄位；連正式供應商時改為真實 key。
# 建議改以 env AI_API_KEY 覆蓋 phytotrack.toml 的 ai.api-key
AI_API_KEY=llama-local-dummy-key
# CORS_ALLOWED_ORIGINS：CORS 白名單（逗號分隔），prod 建議明確列出前端位址
# 例：CORS_ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
# dev 為空沿用 *，prod 為空預設拒絕跨源（同源部署不受影響）
# CORS_ALLOWED_ORIGINS=
# APP_RATE_LIMIT_ENABLED：是否啟用登入/註冊限流（prod 建議 true，test 為 false）
# APP_SECURITY_HEADERS_ENABLED：是否注入 CSP/HSTS 等安全標頭（prod 自動 true）