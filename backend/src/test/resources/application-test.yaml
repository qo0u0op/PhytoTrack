spring:
  datasource:
    # 測試專用：獨立 SQLite 檔案，避免污染開發資料庫 backend/diagnoses.db
    url: jdbc:sqlite:./target/phytotrack-test.db
    driver-class-name: org.sqlite.JDBC
    hikari:
      connection-test-query: SELECT 1
      maximum-pool-size: 1
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    open-in-view: false
  sql:
    init:
      mode: always
      # 測試以業務初始基準建庫（含參照種子、無作物／業務資料）
      schema-locations: classpath:schema-baseline.sql

app:
  rate-limit:
    enabled: false
  cors:
    allowed-origins: ""
  security-headers:
    enabled: false
