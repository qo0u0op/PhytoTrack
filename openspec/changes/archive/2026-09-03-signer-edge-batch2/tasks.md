## 1. 後端硬化

- [x] 1.1 新增名稱正規化共用函式並套用全部查重，驗證大小寫/全半形變體回 409
- [x] 1.2 併發冪等（部分唯一索引或異常轉 409），驗證併發測試只存一筆
- [x] 1.3 `bindToUser` 名實一致檢查，驗證不一致被拒
- [x] 1.4 停用重建循環改為提示啟用舊筆，驗證不累積同名新筆
- [x] 1.5 最後 `active` 停用保護，驗證清空前被阻擋或需確認

## 2. 前端與驗證

- [x] 2.1 `CaseFormView` 簽名人選項顯示身分別與帳號，驗證同名可辨且以 id 提交
- [x] 2.2 新增 `SignerEdgeBatch2Test` 覆蓋上述場景並驗證全綠
- [x] 2.3 全量回歸並驗證既有簽名人測試無退化
