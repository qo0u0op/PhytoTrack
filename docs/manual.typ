#set page(margin: (x: 1.8cm, y: 1.8cm),)
#set text(font: ("Noto Sans CJK TC", "Noto Serif CJK TC"),
  size: 10pt,
  lang: "zh",
  region: "tw",)
#set heading(numbering: "1.1")
#set par(justify: true)

#align(center)[
  #text(size: 16pt, weight: "bold")[PhytoTrack 系統操作手冊]\
  #v(0.2em)
  #text(size: 11pt)[農作物病蟲害診斷諮詢服務系統]\
  #v(0.5em)
  #text(size: 9pt, fill: gray)[本手冊適用對象：診斷站現場工作人員 (非技術人員)]
]

#line(length: 100%, stroke: 0.5pt)

#outline(title: "目錄", indent: auto)

#pagebreak()

= 認識系統

PhytoTrack 是一套用來記錄「農作物病蟲害診斷案件」的電腦系統。工作人員在
網頁上登入後，可以：

- 建立新的診斷案件 (記錄送件人、作物、症狀等資訊)
- 讓 AI 根據輸入的症狀，提供初步的診斷與防治建議
- 查詢、修改、刪除已建立的案件

本系統為網頁版，開啟瀏覽器輸入網址即可使用，不需要安裝任何軟體。

== 帳號說明

系統有三種帳號，權限不同：

- *檢視者 (Viewer)*：只能查看案件，不能新增或修改。
- *診斷員 (Staff)*：可以新增案件、修改案件，並使用 AI 診斷。
- *管理者 (Admin)*：除了診斷員的功能外，還可以刪除案件與管理使用者帳號。

首次開通系統時，站長會提供每個人一組帳號與密碼。

== 登入與登出

1. 打開瀏覽器，輸入系統網址 (由站長提供)。
2. 點選右上角「登入」。
3. 輸入帳號與密碼，點「登入」。
4. 使用完畢請點右上角「登出」，避免他人誤用您的帳號。

= 查看統計儀表板

登入後進入的首頁即為統計儀表板，可快速掌握整體案件狀況：

- *案件總數／本月新增／待處理*：以卡片顯示數字；「本月」依案件收件日期計算。
- *案件狀態比例*：以進度條顯示待處理、已診斷、已結案各佔比例。
- *常見作物 (Top 5) 與常見病蟲害 (Top 5)*：列出件數最多的作物與病蟲害分類。
- *近 6 月案件趨勢*：列出最近 6 個月 (含本月) 每月案件數。

儀表板另顯示 AI 模型 (llama.cpp) 的連線狀態：「已連線」代表可正常使用 AI
診斷；「未連線」時 AI 診斷暫時無法使用。

= 建立診斷案件

診斷員或管理者登入後：

1. 點選上方選單「案件管理」→ 點「建立案件」。
2. 依序填寫各欄位：

#table(columns: (30%, 70%),
  inset: 5pt,
  align: horizon,
  stroke: 0.5pt,
  table.header([欄位], [填寫說明]),
  [送件人資料], [姓名、電話、身分別 (農民／農藥商／其他)、縣市鄉鎮、地址。若同一人再次送件，系統會自動帶出已填過的資料。],
  [作物], [從下拉選單選擇作物種類，例如「柑橘」；找不到時可選擇「其他」。],
  [被害部位], [可勾選多項，例如「葉」與「果」同時被害。],
  [病蟲害分類], [依「病害／蟲害／雜草害」等類別勾選，可多選。],
  [種植面積／被害面積], [文字描述，例如「2 分地」「約 3 成」。],
  [病害情形描述], [以文字描述您看到的症狀，越詳細 AI 診斷越準確。],
  [防治建議與簽名], [可勾選已建議的防治方式，並勾選負責的診斷員姓名。],)

3. 填寫完成後點「儲存案件」。

== 使用 AI 診斷

診斷員或管理者可以在填寫表單時使用 AI：

1. 完成「作物」「被害部位」「病蟲害分類」「病害情形描述」等欄位。
2. 點「AI 診斷」按鈕。
3. 系統會請 AI 根據症狀提出可能的病蟲害與防治建議，並以彈出視窗顯示。
4. AI 建議僅供參考，正式診斷仍需由診斷員依專業判斷確認。

*注意*：AI 診斷需要站長先啟動 AI 服務；若畫面顯示「未連線」，表示 AI
暫時無法使用，仍可直接儲存案件，之後再行補充。

= 查詢與管理案件

== 查看案件列表

點選「案件管理」即可看到全部案件，依收件日期排序。可使用下方按鈕翻頁。

== 篩選案件

案件很多時，可先縮小範圍再查詢。列表上方的篩選工具列（預設收合，點「篩選」展開）可依下列條件組合，版面依 5 列換行排列：

- *第 1 列*：收件日期起、收件日期迄、狀態（待處理／已處理／已結案）
- *第 2 列*：田區縣市、田區鄉鎮、送件人（姓名／顯示名稱／電話 部分比對）、身分別
- *第 3 列*：服務類別、送件方式、耕種方式
- *第 4 列*：作物類別、作物、被害部位
- *第 5 列*：害物、害物類別、建議類別

填好條件後點「篩選」，即可看到符合所有條件的案件；想重新查詢時點「清除」
回到未篩選狀態。多個條件同時設定時，系統會同時套用 (全部符合才會顯示)。篩選結果亦會影響「匯出 CSV」的範圍（匯出即為當前景篩選的全量結果）。若有套用篩選，進入檢視或編輯後按上一頁返回時，篩選條件、分頁（每頁筆數與頁碼）、排序與卡片開啟狀態皆保持，且重新整理後仍由網址還原。

== 檢視案件內容

在列表點「檢視」，會開啟該案件的*明細頁*，顯示完整內容 (送件人、作物、
症狀、防治建議、簽名人與建立時間)。

明細頁上方有兩個按鈕：

- *列印*：以診斷記錄表樣式輸出，方便放入紙本病歷。列印時只會印出診斷單，
  不會印出網頁選單與按鈕。
- *匯出 CSV*：下載包含所有案件欄位的 CSV 檔 (可用 Excel 開啟，中文正常顯示)。
  診斷員或管理者還可在明細頁執行「AI 診斷」，依本案件內容即時取得初步診斷
  與防治建議 (僅供參考，正式診斷仍由診斷員確認)。

== 修改案件

診斷員或管理者在列表點「編輯」，修改欄位後點「儲存案件」。

案件狀態說明：案件建立時為「待處理」。編輯案件時可一併變更狀態——診斷員可將
「待處理」改為「已處理」；「已結案」僅管理者可選。案件一旦「已結案」即不可
再變更狀態 (對診斷員，列表的「編輯」按鈕呈灰色、無法開啟；僅管理者仍可編輯
已結案案件的其他欄位，但狀態維持「已結案」)。列表的「狀態」欄位與篩選均以
此標示。

== 刪除案件

僅管理者可刪除。點「刪除」並在確認視窗中再次確認。刪除後無法復原，請謹慎操作。

= 參照資料管理 (僅管理者)

分頁籤依序為縣市、鄉鎮市區、身分別、服務類別、送件方式、耕種方式、作物類別、被害部位、防治建議（其餘接續）。縣市與鄉鎮市區可新增／修改／刪除，鄉鎮需歸屬縣市；已被案件或送件人引用的項目刪除時會被拒絕。

= 使用者管理 (僅管理者)

1. 點選上方「使用者管理」。
2. 可依帳號、顯示名稱或信箱搜尋；預設僅列出啟用中的帳號，勾選「顯示已停用」可一併查看停用帳號。

新增帳號：使用「註冊」功能建立檢視者帳號後，如需調整角色請洽系統管理者。註冊表單的錯誤訊息（如帳號或信箱重複）會直接顯示在對應欄位下方。

= 常見問題

#set enum(numbering: "1.")
+ *忘記密碼怎麼辦？* 請洽管理者重設。
+ *AI 診斷按下去沒有反應？* 請確認 AI 服務是否已啟動，或稍後再試。
+ *登入後看不到「案件管理」？* 您的帳號可能是「檢視者」，只能查看案件，無法新增或修改。請與管理者確認權限。
+ *不小心刪除案件？* 刪除無法復原，請於刪除前再三確認。

#v(1em)

#line(length: 100%, stroke: 0.5pt)

#align(center)[
  #text(size: 8pt, fill: gray)[PhytoTrack 操作手冊 — 版本 1.6]
]